RENDERING
    COLLISION_DETECTION:
        1. Visualization code is not well written. lots of redundant operations.
        2. DATA IS NOT CENTRALIZED THERE ARE COPIES OF BBOXES FOR VISUALIZATION AND THE ACTUAL LOGIC
    PBR:
        2.The specular component of the radiance seems too flashy.
    SHADOWS:
        1.Cost exceeds available time.(70 render passes)
DATA_ORGANIZATION:
    MODEL_REMOVAL: There is no such mechanism, having creative seizures while thinking of it. better keep it simple.
SKINNING: 
    
    2.VOLUME LOSS ON FOREARM(either skinning or vertex weights)

MISC
    1. At some point the humanoid refused rendering. The problem was that I have changed the mesh code significantly without reflecting the changes in the shader




SOLVED:
    For SceneElements bounding boxes are not consistent and extend themselves to infinity[UNINITIALIZED VARIABLE]
    The arms of the model has some faulty weights or there is an inconsistency that occurs around that area[MATRIX OPERATION ORDER(STILL A MISTERY)]; 